/// A class that contains commonly used HTTP headers for the REST API client.
class RestApiClientHeaders {
  /// A constant string representing the MIME type for multipart form data.
  ///
  /// This is often used when uploading files along with other form data
  /// in a single request.
  static const multipartFormData = 'multipart/form-data';
}
