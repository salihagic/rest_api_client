name: example
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rest_api_client:
    path: ../

dev_dependencies:

flutter:
  uses-material-design: true